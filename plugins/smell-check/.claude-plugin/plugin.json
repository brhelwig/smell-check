{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "smell-check",
  "displayName": "Smell Check",
  "version": "0.2.3",
  "description": "A catalogue of LLM and agent anti-patterns, and the corrections for them",
  "author": {
    "name": "Brandon Helwig",
    "email": "smell-check@brandonhelwig.com"
  },
  "homepage": "https://github.com/brhelwig/smell-check",
  "repository": "https://github.com/brhelwig/smell-check",
  "license": "MIT",
  "keywords": [
    "anti-patterns",
    "agents",
    "workflow",
    "skills"
  ],
  "userConfig": {
    "autoload": {
      "type": "boolean",
      "title": "Load at session start",
      "description": "Inject the catalogue entry point into every session automatically. On by default, because a catalogue nobody loaded corrects nothing. Turn it off to pay the context cost only in sessions that ask for it, and reference the skill from CLAUDE.md instead.",
      "default": true
    }
  }
}
